include "demo.conf"

lasius {
    title = Lasius
    instance = Local Dev
    security = {
        oauth-2-provider {
            enabled = true
            allow-register-users = true
            jwt-token = {
                lifespan = PT30S # 10 seconds
            }
        }
    }
}