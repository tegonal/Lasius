import { useApiProxy } from '~/hooks/use-api-proxy'

/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Lasius API
 * Track your time
 * OpenAPI spec version: 2.2.2+43-e7b745c1+20260321-2204
 */
import {
	type GetAggregatedStatisticsByProjectParams,
	type GetProjectBookingListParams,
	type ModelsBooking,
	type ModelsBookingStats,
} from '../../lasius'
export function useGetAggregatedStatisticsByProject() {
	return useApiProxy<
		ModelsBookingStats[],
		undefined,
		{
			orgId: string
			params?: GetAggregatedStatisticsByProjectParams
			projectId: string
		}
	>({
		getUrl: ({ orgId, params, projectId }) =>
			getGetAggregatedStatisticsByProjectUrl(orgId, projectId, params),
		method: 'GET',
	})
}

export function useGetProjectBookingList() {
	return useApiProxy<
		ModelsBooking[],
		undefined,
		{ orgId: string; params?: GetProjectBookingListParams; projectId: string }
	>({
		getUrl: ({ orgId, params, projectId }) =>
			getGetProjectBookingListUrl(orgId, projectId, params),
		method: 'GET',
	})
}
function getGetAggregatedStatisticsByProjectUrl(
	orgId: string,
	projectId: string,
	params?: GetAggregatedStatisticsByProjectParams,
) {
	const normalizedParams = new URLSearchParams()
	Object.entries(params || {}).forEach(([key, value]) => {
		if (value !== undefined) {
			normalizedParams.append(key, value === null ? 'null' : value.toString())
		}
	})
	const query = normalizedParams.toString()
	return query
		? `/organisations/${orgId}/projects/${projectId}/bookings/stats/aggregated` +
				'?' +
				query
		: `/organisations/${orgId}/projects/${projectId}/bookings/stats/aggregated`
}

/**
 * @summary Get statistics about bookings for the selected project within the selected timeframe
 */
function getGetProjectBookingListUrl(
	orgId: string,
	projectId: string,
	params?: GetProjectBookingListParams,
) {
	const normalizedParams = new URLSearchParams()
	Object.entries(params || {}).forEach(([key, value]) => {
		if (value !== undefined) {
			normalizedParams.append(key, value === null ? 'null' : value.toString())
		}
	})
	const query = normalizedParams.toString()
	return query
		? `/organisations/${orgId}/projects/${projectId}/bookings/history` +
				'?' +
				query
		: `/organisations/${orgId}/projects/${projectId}/bookings/history`
}
